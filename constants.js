export const URL = Object({
    //API_URL : process.env.NODE_ENV === "production" ? "https://git.heroku.com/jiho-market-app.git" : "http://127.0.0.1:8080",
    ImageUrl : ``,
    
})

export const variables = Object({
    initialApproach : true,
})